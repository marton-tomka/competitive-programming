#include <bits/stdc++.h>
using ll = long long;

void solve() {
    ll n, L;
    std::cin >> n >> L;
    std::vector<int> arr(n);
    for (int i = 0; i < n; i++)
        std::cin >> arr[i];

    // ... count how many forced moves were there after greedy pairing

    std::println("{}");
}
